controller:
  serviceType: ClusterIP   # 👈 aquí sí se usa serviceType
  adminUser: admin
  adminPassword: supersecret
volumeMounts:
  - name: docker-sock
    mountPath: /var/run/docker.sock
volumes:
  - name: docker-sock
    hostPath:
      path: /var/run/docker.sock
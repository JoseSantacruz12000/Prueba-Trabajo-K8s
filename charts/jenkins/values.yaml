controller:
  serviceType: ClusterIP 
  jenkinsUser: admin
  jenkinsPassword: supersecret

  resources:
    limits:
      cpu: "500m"
      memory: "1Gi"
    requests:
      cpu: "250m"
      memory: "512Mi"
